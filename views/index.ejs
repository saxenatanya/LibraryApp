mIDDEL

<h2>Recentlu added</h2>

<div>
    <% books.forEach(book =>{ %>
        <a href="/books/<%= book.id %>">
<img  height="150" width="100" src="<%= book.coverPathImage %>">
</a>
   <% }) %>
</div>